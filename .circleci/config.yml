version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.9
        environment:
          ANYTHING: https://api.civic.com/sip-sec-dev/DEV

    working_directory: ~/repo

    steps:
      - checkout

      # Download and cache dependencies
      - restore_cache:
          keys:
          - v1-dependencies-{{ checksum "package.json" }}
          # fallback to using the latest cache if no exact match is found
          - v1-dependencies-

      - run:
          command: |
            npm install

      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package.json" }}

      - run: npm run ci

workflows:
  version: 2
  build-deploy:
    jobs:
      - build
